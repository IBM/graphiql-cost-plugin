/// <reference types="react" />
import { Characteristics } from '../types';
declare type Props = {
    characteristics: Characteristics;
};
export default function CostOnly({ characteristics }: Props): JSX.Element;
export {};
